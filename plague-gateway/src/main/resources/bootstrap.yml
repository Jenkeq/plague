#端口
server:
  port: 9099

spring:
  cloud:
    nacos:
      discovery:
        server-addr: 124.220.173.67:8848
        username: nacos
        password: nacos
        namespace: plague-dev
        group: PLAGUE
      config:
        server-addr: 124.220.173.67:8848
        # 默认扩展名为 properties
        prefix: ${spring.application.name}
        file-extension: yaml
        group: PLAGUE
        namespace: plague-dev
  # 配置文件
  profiles:
    active: dev
  # 应用名
  application:
    name: plague-gateway